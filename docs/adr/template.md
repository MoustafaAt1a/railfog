# ADR-NNNN — <short title>

Status: Proposed | Accepted | Superseded by ADR-NNNN Date: <YYYY-MM-DD> Raised
by: architect | reviewer | security-auditor | human

## Context

What gap in `docs/contracts/` forced this decision? Cite the task ID that
surfaced it. If this is resolving a contradiction between two contract clauses,
name both.

## Decision

State it as a rule, the same way `docs/contracts/*.md` states rules — not as a
discussion. One sentence if possible.

## Alternatives considered

At least one. Say why it was rejected — "more complex" is not sufficient on its
own; tie it to an Engineering Rule (`AGENTS.md` §3) or a spec principle.

## Consequences

- What contract file needs updating as a result of this ADR, and which clause
  ID(s).
- What existing code (if any) is now non-compliant and needs a follow-up task.

## Spec references

Clause IDs consulted while making this decision (may be "none — this is a
genuine gap").
